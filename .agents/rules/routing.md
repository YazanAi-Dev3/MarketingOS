# Routing Rule

Before repository mutation, classify work as CHAT, MICRO, STANDARD, or HEAVY using `.agents/workflow/ROUTING.md`. STANDARD and HEAVY Builders use native `workspace=branch`. Keep at most two active subagents by policy. Builder and Reviewer must remain epistemically independent.
