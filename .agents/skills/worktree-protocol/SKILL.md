---
name: worktree-protocol
description: Applies Marketing OS Git trust and Antigravity native branch-worktree isolation rules.
---
# Worktree Protocol
Read `.agents/workflow/WORKTREES.md`. For STANDARD/HEAVY invoke the Builder with `workspace=branch`. Never place two writers in one worktree. Main/human founders own integration decisions; no agent auto-merges `main`.
