# First Run Checklist

- [ ] `git status` is understood and trusted main is clean/known.
- [ ] `agy` is authenticated and the requested model is available.
- [ ] six custom roles are discovered.
- [ ] three workspace rules are set Always On.
- [ ] `python scripts/verify_control_plane.py` passes.
- [ ] `python scripts/test_hooks.py` passes.
- [ ] `python scripts/scan_for_secrets.py` passes.
- [ ] AG-CC-02 model/effort inheritance is proven or pinned runner chosen.
- [ ] AG-CC-03 native branch-worktree review path is proven or explicit Git worktree fallback chosen.
- [ ] no raw Clients Hunter archive/database/credentials are copied into the new repository.
- [ ] first real product task starts with `T-08`, then `T-01`; source adapters do not begin before their prerequisite current-site checks.
