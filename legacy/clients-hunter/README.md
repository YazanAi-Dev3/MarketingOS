# Clients Hunter — Donor Codebase Policy

The original `Clients-Hunter.zip` is **not included** in this package because it contains legacy runtime artifacts and historically included credential-bearing files. Keep the original outside the new repository and treat it as quarantined source material.

The new project does not run Clients Hunter. It selectively migrates useful behavior/knowledge into the new architecture under `A-038`.
